package homework;

import javax.swing.JOptionPane;
/*
public class huiwen {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		String input = JOptionPane.showInputDialog("请输入字符串"); //显示一个有输入框的对话框
		boolean y= true; //记录判断结果
		for(int i=0; i<input.length()/2;i++) {
			if (input.charAt(i) != input.charAt(input.length()-1-i)) {//选取字符串中正数第i个和倒数第i个字符匹配，若不匹配则标记匹配失败并跳出循环
				y = false;
				break; 
			}	
		}
		if(y == true) { //如果标记是匹配成功
			JOptionPane.showMessageDialog(null, input+"是回文");  
		}
		   
		else//如果标记是匹配失败
			JOptionPane.showMessageDialog(null, input+"不是回文","message",JOptionPane.WARNING_MESSAGE);
		
			

	}

}
*/